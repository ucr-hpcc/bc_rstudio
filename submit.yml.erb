batch_connect:
  template: "basic"
  conn_params:
    - csrftoken

script:
  native:
    - "-c"
    - "<%= num_cores %>"
    - "--mem"
    - "<%= num_mem %>GB"
    - "-p"
    - "<%= partition %>"
    - "-t"
    - "<%= num_days.to_i %>-00"
